# Framework & Core
Django>=5.0,<5.1
django-environ
psycopg2-binary
requests

# PDF Generation & Signing
WeasyPrint
pyhanko[crypto]
pillow # Para procesar imágenes en la generación de PDFs.
qrcode # Para generar códigos QR.

# Frontend & UI
django-htmx
django-widget-tweaks

# Security
django-axes  # Protección fuerza bruta
django-csp # Para gestionar Content Security Policy (evitar XSS).
django-encrypted-model-fields # Para cifrar datos sensibles en la BD (ej. tokens de API).
django-two-factor-auth # Para implementar el 2FA obligatorio de Fiscales y Firmantes.
django-otp # Para implementar el 2FA obligatorio de Fiscales y Firmantes.
